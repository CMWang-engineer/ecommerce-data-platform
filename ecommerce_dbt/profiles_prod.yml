ecommerce_dbt:
  target: prod
  outputs:
    prod:
      type: duckdb
      path: '/tmp/ecommerce_prod.duckdb'
      schema: main
      threads: 4
      extensions:
        - parquet
        - httpfs
      config:
        enable_hive_partitioning: true
